{
  "version": 2,
  "buildCommand": "yarn prisma generate && next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}